import { config } from '@things-factory/env'

export const acceptStoreOrderCancellation = {
  async acceptStoreOrderCancellation(_: any, {}, context: any) {
    // only in Shopee, to accept buyer order cancellation
  }
}
