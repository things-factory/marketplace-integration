import { config } from '@things-factory/env'

export const setStoreOrderStatus = {
  async setStoreOrderStatus(_: any, {}, context: any) {
    // set status to pack by marketplace (Lazada) to get tracking numbers
    // set status to ready to ship (Lazada)
  }
}
